#include "main.h"
/**
 * mul - Multiplies two integers
 * @m: The first integer
 * @n: The second integer
 * Return: Always 0 (Success)
 */

int mul(int m, int n)
{
	int result = m * n;

	return (result);
}
